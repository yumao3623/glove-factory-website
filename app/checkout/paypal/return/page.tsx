import { PayPalReturn } from "@/components/commerce/quote-checkout";
export const metadata = { title: "Payment confirmation", robots: { index: false, follow: false } };
export default async function Page({ searchParams }: { searchParams: Promise<{ orderId?: string; token?: string }> }) { const p = await searchParams; return <main id="main-content" className="mx-auto min-h-[65vh] max-w-[1100px] px-5 py-16 sm:px-8"><p className="section-label text-stone-500">JS Meilai | Payment</p><h1 className="mt-4 font-serif text-4xl text-[#0d2b3f]">Payment confirmation</h1><PayPalReturn orderId={p.orderId ?? ""} paypalOrderId={p.token ?? ""} /></main>; }
