# JS Meilai 设计系统

状态：2.0 工作版设计系统（`V2_WORKING_SYSTEM`）

## 设计方向

这是一个面向海外采购商、婚礼与礼服团队、戏服供应商，以及委托定制生产的品牌的 B2B 场合服饰产品目录。整体视觉语言为**编辑式实用主义（Editorial Utility）**：产品图片主导展示，导航保持克制，每个页面都为采购方提供明确的下一步。

主要参考是本地 Apple DESIGN.md 设计分析。其中以摄影为主的产品叙事、明暗交替的背景、克制的界面装饰和统一的操作强调色，适合高品质产品目录。JS Meilai 针对工厂场景改用这些设计方式：事实规格、系列比较、筛选、询价背景和运营状态优先于戏剧化展示。不复制 Apple 的品牌资产、文案、字体或代码。

2.0 的 Taste 参数：设计差异度 `DESIGN_VARIANCE: 7`、动效强度 `MOTION_INTENSITY: 4`、视觉密度 `VISUAL_DENSITY: 5`。

## 设计变量

| 角色 | 变量 | 取值 | 用途 |
| --- | --- | --- | --- |
| 墨色 | `--ink` | `#111416` | 标题、主要文字、深色操作控件 |
| 柔和墨色 | `--ink-muted` | `#5c676d` | 辅助文案和元数据 |
| 海军蓝 | `--navy` | `#0d2b3f` | 品牌主色、导航、后台重点内容 |
| 基础背景 | `--canvas` | `#f5f6f5` | 网站和表单背景 |
| 暖色背景 | `--canvas-warm` | `#f7f5f1` | 产品目录和编辑式内容区 |
| 珍珠白 | `--pearl` | `#ffffff` | 卡片、输入框、图片展示区 |
| 细分隔线 | `--hairline` | `#c9ced0` | 分隔线和表格线 |
| 操作强调色 | `--action` | `#0d2b3f` | 唯一的实心操作强调色 |
| 操作控件文字 | `--on-action` | `#ffffff` | 操作强调色背景上的文字 |

系统使用一种深蓝操作强调色，搭配冷暖中性色背景。不使用装饰性渐变、发光效果或无关的强调色。成功和错误的语义颜色仅用于系统反馈与后台状态。

## 字体

- 展示字体：`Iowan Old Style, Baskerville, Georgia, serif`，用于编辑式标题和产品名称。它与高级场合服饰的传统气质相符，并搭配克制的无衬线界面文字。
- 界面与正文：`Helvetica Neue, Arial, sans-serif`，行高为 1.45–1.6。
- 眉题与标签：使用 10–11px 的大写字母，并明确设置字距。
- 标题使用紧凑行高，但不能裁切斜体字的下伸部分。
- 后台和未来订单页面中的数值使用等宽数字。

## 布局原则

1. 桌面端使用 12 列网格，移动端使用单列内容流。内容最大宽度为 1,280–1,400px。
2. 产品图片是主要视觉内容。使用一张大图配合辅助细节，避免灰色占位框。
3. 每个页面设置一个明确的主要操作。次要操作使用文字链接或克制的描边控件，并保持相同的最小点击区域。
4. 六个产品系列在所有位置都必须完整：婚礼手套（bridal gloves）、长筒/晚宴手套（opera/evening gloves）、戏服/舞台手套（costume/stage gloves）、儿童礼服手套（kids dress gloves）、婚礼头纱（wedding veils）和袖套（arm sleeves）。
5. 搜索和筛选是产品目录的基础能力。`/products/` 必须显示搜索框；筛选状态必须能通过 URL 访问，并适用于移动端。
6. 未确认的业务事实以英文“待确认”（To confirm）或“按需提供”（Available on request）展示，不能虚构价格、库存、起订量或交期。

## 组件状态

- 按钮与链接：默认、悬停、键盘聚焦、按下、禁用和加载状态必须可区分，不能只依靠颜色。
- 筛选项：已选、未选和当前无产品的选项都显示数量。空选项保持可见，未来增加产品时无需重写界面。
- 产品媒体：加载中、已批准、缺失和私有存储错误分别提供对应文案。私有数据库资产不能被假图片静默替换。
- 后台：草稿、已上架、已归档、待复核和服务商不可用状态使用文字标签与颜色共同表达。
- 表单：必须提供行内校验、提交进度、成功反馈，以及服务商出错时停止操作的错误状态。

## 动效

动效用于说明层级关系，包括 CSS 公告滚动条、短暂的图片悬停变换和筛选抽屉过渡。公告条在悬停或聚焦时暂停，并在用户启用减少动态效果（`prefers-reduced-motion`）时关闭。GSAP 预留给未来需要统一编排的产品叙事或随滚动展开的工厂展示，简单过渡不引入 GSAP。

## 响应式行为

- 桌面端：常驻页头工具区、四列产品网格、左侧筛选栏、双列商品详情。
- 平板端：三列产品网格、收紧的筛选栏；空间允许时使用双列商品详情。
- 移动端：菜单面板、可见的搜索（Search）和账户（Account）链接、双列产品卡片、筛选抽屉、上下排列的商品图库与详情，以及至少 44px 的触控区域。
- 内容型首屏不使用 `h-screen`；尺寸由内容决定，必要时使用 `min-height`。

## 无障碍与内容边界

所有公开界面文案均为英文。中文来源术语仅保留在导入代码和证据记录中。图片必须来自已批准的资产清单或经过认证的 Supabase 对象存储。视觉参考和竞品截图不能直接作为生产内容。正式上线验收通过前，公开网站保持禁止索引（`noindex`）。
