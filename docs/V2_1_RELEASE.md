# JS Meilai 2.1 发布记录

版本：`2.1.0`。基线：`c7a83fe`。状态：功能与受控验收完成，生产发布记录待部署后补充。

## 授权与检查点

2026-09-18 用户授权：完成 2.0 未完成项；恢复 `yumao3623@gmail.com` 的登录能力；新增 `15795752910@163.com` 为管理员；以工厂营业执照开通方便的海外支付方案；完成后提交、推送和部署。

当前工作流为 **W04 — 2.1 账号、询价与报价支付闭环**。这是在已完成 W02 产品、W05 视觉与 W10 仓库基线上的后续版本，覆盖必要的账号、后台、邮件、报价、支付返回及生产配置。此次明确授权扩展了历史 V1 路线图中的外部操作范围；历史门槛及验收记录不改写。

选择 PayPal 中国企业账户及全球收单。用户随后明确说明工厂属于阿姨，需要次日取得材料，开户暂不能完成。账户实名、密码和协议由经营者/法人在官方页面完成；此次部署 `PAYPAL_CHECKOUT_ENABLED=false`，不把代码或模拟交易当作真实商户验收。

## 验收范围

- 两个邮箱列入服务端管理员名单，管理员邮箱必须已验证；邀请和恢复不代设密码。
- 忘记密码、邮件确认、设置新密码、登录/退出和受保护后台。
- 后台产品/媒体/规格/库存、询价详情及状态、服务器报价与订单。
- 询价真实持久化、幂等重试、通知邮件与失败状态。
- PayPal 订单归属、已确认商业条款、订单金额、返回捕获及验签/幂等事件。
- 桌面/移动浏览器、自动化检查、远程 Git ref、部署状态与线上路径。

## 发布证据

### 账号

- 两个指定邮箱均在生产服务端管理员名单中；授权仍要求 Supabase 确认邮箱。
- Gmail 恢复邮件和 163 邀请邮件均由 Resend 确认 delivered。用户确认 Gmail 已完成设置；数据库记录其实际登录。163 合伙人尚未激活，不能标记为已登录验收。
- 开发期邮件使用本机地址；部署后为 163 重新发送正式域名的账号链接，避免合伙人在另一台电脑打不开 localhost。
- 新增忘记密码、主动确认邮件、受限密码恢复 Cookie 和设置密码表单。新密码至少 12 位；邮件链接放在 fragment 中，不会作为服务端请求 URL 留存。退出清理会话与报价访问 Cookie。

### 产品、图片与后台

- 继续使用 Taste 与 design-reference 确定的 `DESIGN.md`；补齐后台表单标签、操作导航、可填写的规格与确认状态、列表检索、封面选择和英文上传控件。
- 浏览器验证发现并修复新产品“年龄组待确认”不能保存、多标签逗号输入丢失等问题。
- 36 个真实上架产品保留原状态。137 张展示图片及 36 张历史缩略图全部在 Supabase 私有存储中验证 SHA-256、尺寸和格式；缩略图不关联公开产品。云端清单为 `data/products/media-storage.json`。
- 173 张产品二进制文件移出 Git，保存在本机仓库外 `../product-media-archive-v1/`；公开目录不再回退静态产品，归档与媒体访问由数据库决定。
- 受控浏览器完成草稿创建、图片上传、编辑保存、多颜色/规格保留、库存创建和修改、超量预留拦截、归档；匿名后台、草稿详情和草稿媒体均被阻止。测试产品没有公开发布。

### 询价、报价和订单

- 实际经过“搜索/筛选 → 产品详情 → 定制选项 → 询价清单 → 修改数量 → 提交询价”。选中产品与 12 件数量正确入库，通知邮件 delivered。
- 重复相同请求只保留一条记录；请求限流持久化，跨站提交被拒绝。
- 后台实际展开询价、修改处理状态、带入买家准备报价；报价合计 55 USD 商品加 25 USD 运费为 80 USD，税费为明确输入的 0。
- 私有报价拒绝未知买家与错误令牌，响应不泄露访问哈希。取消报价后立即不可支付。已验证邮箱可以找到注册前发出的对应报价，其他买家不可见。
- PayPal 创建、捕获、验签、金额与币种核验及幂等入账代码就绪；关闭开关时结账返回 503，买家页明确说明尚不能在线付款。没有发生真实交易。

### 浏览器与检查

- 独立 Chromium 浏览器测试桌面 1440px 和移动 390px：后台、目录筛选、产品详情、询价清单、询价表单、报价、账号流程。抽查无横向溢出、无中文页面正文。
- 用户 Chrome 已打开实际 Gmail 管理员页面，但 CUA 在窗口展开后仍只返回标题、无控件与截图。因此完整操作证据来自独立浏览器的临时管理员会话；不能宣称用户 Chrome 的每一个按钮均验收通过。
- `npm run lint`、`npm run typecheck`、`npm test`（60/60）、云端 `npm run validate:products` 和 `npm run build` 均通过。生产构建成功生成 93 个静态页面/端点，动态后台和目录按请求读取服务。
- 另在一次性数据库订单上模拟验证：错误金额拒绝、重复捕获事件只记录一次、退款状态不会被迟到捕获覆盖。此项仅是数据库验证，没有调用支付服务商。
- 测试记录与临时管理员会话清理后，恢复仅两个正式邮箱的本地管理员名单；生产名单从未加入临时账号。

## 明确延后项

- 工厂经营者完成 PayPal 开户、商户审核、收单/结算账户确认，以及真实沙盒和生产支付验收。
- 163 管理员由合伙人亲自打开正式邀请并设置密码。
- Supabase 安全检查仅剩泄露密码保护告警；[官方文档](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)说明需 Pro 及以上套餐，本次没有购买升级。
- 网站保持原来的 noindex 预发布边界，GSC 和可索引正式发布属于后续 SEO 检查点。
